name: Deprecated Hermes Workspace Auto Build

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deprecation
        run: |
          echo "This workflow is deprecated."
          echo "Use .github/workflows/release-to-umbrel-community-store.yml instead."
          exit 1
